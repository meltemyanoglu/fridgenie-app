{
  "name": "fridgenie-vision-worker",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker that proxies fridge photos to Google Gemini Vision.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
